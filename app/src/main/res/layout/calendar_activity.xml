<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingTop="18dp"
        android:paddingEnd="16dp"
        android:paddingBottom="12dp">
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/calendar_horizon"
            android:textColor="@color/gtd_text_primary"
            android:textSize="28sp"
            android:textStyle="bold" />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="@string/calendar_subtitle"
            android:textColor="@color/gtd_text_secondary"
            android:textSize="15sp" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingStart="4dp"
        android:paddingEnd="4dp">
        <TextView style="@style/WeekdayLabel" android:text="@string/weekday_monday" />
        <TextView style="@style/WeekdayLabel" android:text="@string/weekday_tuesday" />
        <TextView style="@style/WeekdayLabel" android:text="@string/weekday_wednesday" />
        <TextView style="@style/WeekdayLabel" android:text="@string/weekday_thursday" />
        <TextView style="@style/WeekdayLabel" android:text="@string/weekday_friday" />
        <TextView style="@style/WeekdayLabel" android:text="@string/weekday_saturday" />
        <TextView style="@style/WeekdayLabel" android:text="@string/weekday_sunday" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/calendarRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingStart="3dp"
        android:paddingEnd="3dp"
        android:paddingBottom="16dp" />
</LinearLayout>
